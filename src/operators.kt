fun main() {
 //Assignment operators
    var x = 90
    x -= 4
    println(x)

    //Logical operators
    var log = 56
    println(log < 100 && log > 89)
    println(log < 100 || log > 89)
    println(!(log < 100 || log > 89))

    //Comparison operators

}